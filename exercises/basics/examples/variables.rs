fn main() {
    // Immutable variables (Default)
    let x = 5;
    println!("x = {x}");

    // Mutable variables
    let mut y = 10;
    println!("y = {y}");
    y += 1;
    println!("y after mutation = {y}");
}